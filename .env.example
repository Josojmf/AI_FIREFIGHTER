# ===========================================
# AI Firefighter - Environment Variables
# ===========================================

# MongoDB Atlas Configuration
MONGO_USER=your_mongo_username
MONGO_PASS=your_mongo_password
MONGO_CLUSTER=cluster0.xxxxx.mongodb.net
DB_NAME=FIREFIGHTER

# JWT Configuration
SECRET_KEY=your-super-secret-key-here-min-32-chars
JWT_EXPIRES_HOURS=24

# VAPID Keys (for push notifications)
VAPID_PUBLIC_KEY=your_public_key
VAPID_PRIVATE_KEY=your_private_key

# ===========================================
# SSO & CORS Configuration
# ===========================================

# Production URL (without port - ports added automatically)
# Examples:
# - http://167.71.63.108
# - https://yourdomain.com
PRODUCTION_URL=http://your-production-ip-or-domain

# ngrok URL (temporary for HTTPS development)
# Update this every time you start ngrok with a new domain
# Example: https://abc-def-ghi.ngrok-free.dev
NGROK_URL=

# ===========================================
# Usage Instructions
# ===========================================

# 1. Copy this file to .env in each service directory:
#    - API/.env
#    - FO/.env (if needed)
#    - BO/.env (if needed)

# 2. Fill in your actual values

# 3. NEVER commit .env files to git (already in .gitignore)

# 4. For ngrok development:
#    - Start ngrok: ngrok http 8000
#    - Copy the HTTPS URL (e.g., https://xyz.ngrok-free.dev)
#    - Update NGROK_URL in API/.env
#    - Restart API server

# 5. For production:
#    - Set PRODUCTION_URL to your server IP or domain
#    - Remove or leave NGROK_URL empty
#    - API will automatically allow CORS from production URL
