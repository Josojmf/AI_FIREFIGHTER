# API/requirements.txt - Backend API dependencies

# Core Flask API
flask==2.3.3
flask-cors==4.0.0
werkzeug==2.3.7

# Database
pymongo==4.5.0

# Security
bcrypt==4.0.1

# Configuration
python-dotenv==1.0.0

# HTTP requests
requests==2.31.0

# Production server
gunicorn==21.2.0

# JWT for authentication
PyJWT==2.8.0          # Cambiado de flask-jwt-extended a PyJWT (que es lo que usas)

# MFA Dependencies
pyotp==2.9.0          # Para generar y verificar códigos TOTP
qrcode[pil]==7.4.2     # Para generar códigos QR
Pillow==10.1.0         # Para procesamiento de imágenes QR