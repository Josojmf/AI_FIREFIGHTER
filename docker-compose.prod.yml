version: '3.8'

services:
  frontend:
    image: ${FRONTEND_IMAGE:-ghcr.io/josojmf/ai_firefighter/frontend:latest}
    ports:
      - "8000:8000"
    environment:
      - DB_USERNAME=${DB_USERNAME}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_CLUSTER=${DB_CLUSTER}
      - NODE_ENV=production
      - API_BASE_URL=http://backend:5000/api
    restart: unless-stopped
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - firefighter-network

  backend:
    image: ${BACKEND_IMAGE:-ghcr.io/josojmf/ai_firefighter/backend:latest}
    ports:
      - "5000:5000"
    environment:
      - DB_USERNAME=${DB_USERNAME}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_CLUSTER=${DB_CLUSTER}
      - NODE_ENV=production
      - FLASK_ENV=production
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    networks:
      - firefighter-network

networks:
  firefighter-network:
    driver: bridge