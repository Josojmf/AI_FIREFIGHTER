# FO/requirements.txt - Frontend Flask application dependencies - VERSIÓN CORREGIDA

# 🔥 CORRECCIÓN CRÍTICA: Versiones compatibles de Flask y Werkzeug
Flask==2.3.3
Werkzeug==2.3.7  # ✅ Versión específica compatible con Flask-Login

# 🔥 CORRECCIÓN: Flask-Login compatible
Flask-Login==0.6.3  # ✅ Versión actualizada que funciona con Werkzeug 2.3.7

# Formularios
Flask-WTF==1.1.1
WTForms==3.0.1

# HTTP requests
requests==2.31.0

# Configuration
python-dotenv==1.0.0

# Security
bcrypt==4.0.1

# AI/ML for question processing
transformers==4.35.0
torch==2.1.0
accelerate==0.24.0

# Database
pymongo==4.5.0

# PDF processing
PyMuPDF==1.23.8  # ✅ CORREGIDO: Usar PyMuPDF en lugar de fitz
fpdf2==2.7.6

# Production server
gunicorn==21.2.0

# 🔥 CORRECCIÓN: Dependencias específicas para evitar conflictos
numpy==1.24.3  # ✅ Versión compatible con transformers y torch
packaging==23.2

# Utilidades adicionales
Jinja2==3.1.2
MarkupSafe==2.1.3
click==8.1.7
itsdangerous==2.1.2
blinker==1.6.3

# Dependencias de AI/ML optimizadas
tokenizers==0.14.1
safetensors==0.4.1
huggingface-hub==0.17.3
filelock==3.13.1

# 🔥 CORRECCIÓN: Dependencias específicas para PDF
Pillow==10.1.0  # Requerida para procesamiento de imágenes en PDFs